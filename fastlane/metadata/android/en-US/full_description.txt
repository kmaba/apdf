convert word, powerpoint, images, text and pdf files into a single pdf, entirely on your device. no network, no accounts, no tracking.

work with any mix of files: merge a photo, a word document and a pdf into one file, in the order you choose. output keeps full, original quality with no re-encoding, and you pick exactly where to save it.

share files from any app: select them, tap share, choose apdf. or add files directly inside the app.
